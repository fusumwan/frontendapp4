import React from 'react';
import { Link } from 'react-router-dom';
import { ComponentProps } from '../../../interfaces/ComponentProps';
const HeaderContentCompoent: React.FC<ComponentProps> = ({appContent}) => {
    return (
        <div>
            {/* Header default specific content here */}
        </div>
    );
};

export default HeaderContentCompoent;
